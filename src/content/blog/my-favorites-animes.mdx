---
author: Dâmocles Gil Marçal
pubDatetime: 2024-02-23T09:22:00Z
title: Favorites animes.
slug: favorites-animes
featured: true
draft: false
tags:
  - anime
description: List of animes I like most 😉
---

import { ANIME_LIST } from "../anime-list.ts";

<div className="grid grid-cols-3 gap-4 items-center text-center">
  {ANIME_LIST.map((item, index) => (
    <a
      key={index}
      href={item.href}
      target="blank"
      className="p-3 pt-2 hover:bg-skin-card no-underline"
      title={item.animeTitle}
    >
      <img
        className="m-auto block"
        width="150"
        src={item.imgSrc}
        alt={`Crunchyroll ${item.animeTitle}`}
      />
      <p className="mt-2 mb-0 text-sm">{item.animeTitle}</p>
    </a>
  ))}
</div>
